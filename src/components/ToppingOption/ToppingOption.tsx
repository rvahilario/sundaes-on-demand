import { Container } from './styles';
import { ToppingOptionProps } from './types';

export const ToppingOption = ({}: ToppingOptionProps) => {
	return (
		<Container>
			<h1>ToppingOption</h1>
		</Container>
	);
};
